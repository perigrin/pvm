# ABOUTME: Requires a module that does not exist, so perl fails for a reason about this machine.
# ABOUTME: Must leave the denominator rather than be blamed on the parser.
use No::Such::Module::Anywhere;
my $x = 1;
