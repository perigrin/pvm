# Minimal Perl project template
name = "minimal"
description = "Minimal Perl project with basic structure"

[directories]
create = ["lib", "t"]

[dependencies]
# Core pragmas (strict, warnings, utf8) are built-in to Perl

[dev_dependencies]
"Perl::Critic" = ""
"Perl::Tidy" = ""

[test_dependencies]
"Test::More" = ">= 0.98"
"Test::Exception" = ""

[config]
# Project-specific configuration can go here

[gitignore_additions]
# Additional .gitignore entries specific to this template
